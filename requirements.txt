# Runtime dependencies
send2trash>=1.8

# Development / test dependencies (install with: pip install -r requirements.txt pytest)
# pytest>=8
# pyinstaller>=6   # only needed to build the standalone .exe
